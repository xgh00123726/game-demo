using UnityEngine;
namespace GameBase.Tools
{
    public class MoveSysController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
