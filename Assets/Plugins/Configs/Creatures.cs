using UnityEngine;

namespace GameBase.Infos
{
    public static class Creatures
    {
        public static bool healBarDebugMode = false;
        public static Vector3 healthBarOffset = Vector3.zero;
    }
}
